const REQUEST_USER_KEY = 'user';

const TOGGLE_VALUE = {
  OFF: 0,
  ON: 1,
};

const TOKEN_TYPE = {
  FORGOT_PASSWORD: 'forgot password',
  VERIFY_OTP: 'verify otp',
};

module.exports = {
  REQUEST_USER_KEY,
  TOGGLE_VALUE,
  TOKEN_TYPE,
};
